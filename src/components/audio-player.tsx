import {Dispatch, SetStateAction} from "react";


type AudioPlayerProps = {
    trackUrl: string;
};

export default function MusicPlayerTrackListComponent({trackUrl} :  AudioPlayerProps) {
    return (
        <p>hi</p>
    );
}